<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.alfirex.acceso_a_datos_actividad_tema_4_quicktrade">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegistroActivity" />
        <activity android:name=".DiversasAccionesActivity" />
        <activity android:name=".PerfilActivity" />
        <activity android:name=".addProductoActivity" />
        <activity android:name=".modificarProductoActivity" />
        <activity android:name=".BorrarProductoActivity" />
        <activity android:name=".BusquedaPorCategoriaActivity" />
        <activity android:name=".BusquedaProdUserActivity2" />
        <activity android:name=".FavoritoActivity" />
        <activity android:name=".MostrarFavActivity"></activity>
    </application>

</manifest>